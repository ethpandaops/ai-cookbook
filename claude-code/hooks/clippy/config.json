{
  "name": "clippy",
  "description": "Run Rust clippy linter after editing Rust files to catch common mistakes",
  "hook_type": "PostToolUse",
  "matcher": "Write|Edit",
  "requirements": {
    "commands": ["cargo"],
    "description": "Requires Rust toolchain with clippy installed (rustup component add clippy)"
  }
}