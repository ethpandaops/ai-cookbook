{
  "name": "rustfmt",
  "description": "Automatically format Rust files with rustfmt after editing",
  "hook_type": "PostToolUse",
  "matcher": "Write|Edit",
  "requirements": {
    "commands": ["rustfmt"],
    "description": "Requires Rust toolchain with rustfmt installed (rustup component add rustfmt)"
  }
}
