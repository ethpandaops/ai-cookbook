{
  "name": "ethereum-specs",
  "description": "Access Ethereum specifications for consensus layer (phase0-fulu) and execution layer (Engine API, JSON-RPC methods)",
  "implementation": "tools/mcp-servers/ethereum-specs",
  "entry_point": "bin/ethereum-specs-mcp",
  "version": "2.0.0",
  "runtime": "binary",
  "build_command": "make build",
  "config_prompts": {
    "auto_update": {
      "prompt": "Auto-update specs on startup",
      "default": "true",
      "description": "Pull latest specs from GitHub when MCP server starts"
    },
    "specs_branch": {
      "prompt": "Specs branch to track",
      "default": "master",
      "description": "Branch of ethereum/consensus-specs to use (dev, master, etc)"
    },
    "execution_apis_branch": {
      "prompt": "Execution APIs branch to track",
      "default": "main",
      "description": "Branch of ethereum/execution-apis to use"
    }
  }
}
